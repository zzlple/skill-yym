---
name: 油羊毛
description: 加油优惠面板（汽油/柴油优惠券聚合）。当用户提到「加油优惠 / 油价优惠 / 加油券 / 中石油 / 中石化 / 易捷加油 / 中油好客 e 站 / 95 号 98 号优惠 / 加油羊毛 / 油羊毛 / oil-coupon」时使用。技能会自动在本地启动一个小型 Web 服务，并在浏览器打开面板，把中国石油、中国石化以及第三方渠道的加油优惠按【时间 / 地区 / 活动链接 / 活动入口二维码 / 适用油号 / 使用范围】以表格形式列出，支持按品牌与 92 / 95 / 98 号油筛选，覆盖全国通用、湖北、陕西三档地区。
---

# 油羊毛（oil-coupon）· 加油优惠面板 Skill

## 1. 技能用途

把散落在官网、公众号、银行活动页里的加油优惠，聚合成本地网页面板，一眼看清：

- 品牌维度：**中国石油** / **中国石化** / 两桶油均可 / 第三方渠道（云闪付、支付宝、微信、高德、滴滴）
- 油号维度：**92# / 95# / 98#**
- 地区维度：**全国通用 / 湖北 / 陕西**
- 每条优惠展示：时间、地区、优惠内容、适用油号、**活动链接**、**活动入口二维码**、**优惠券支持使用的范围**

## 2. 目录结构

```
油羊毛/
├── SKILL.md               本说明
├── data/coupons.json      优惠数据源（唯一需要维护的文件）
├── scripts/oil_server.py  本地面板服务（HTTP + 二维码生成 + 单实例复用）
├── scripts/start_panel.ps1 一键启动脚本（自动找 Python、后台起服务、开浏览器）
└── web/index.html         面板前端（筛选 + 表格 + 二维码）
```

## 3. 启动方式（接到用户指令后照此执行）

### 方式 A（推荐，一条命令）
```powershell
powershell -ExecutionPolicy Bypass -File "<SKILL_ROOT>\scripts\start_panel.ps1"
```
脚本会：定位 Python → 已运行则直接复用并打开浏览器 → 未运行则后台启动 → 轮询健康检查 → 打开默认浏览器 → 打印面板地址。

### 方式 B（直接起服务，需要后台执行，勿阻塞）
```powershell
$py = (Get-Command python).Source
Start-Process -FilePath $py -ArgumentList '"<SKILL_ROOT>\scripts\oil_server.py"','--open' -WindowStyle Hidden
```

### 常用参数
| 参数 | 说明 |
| --- | --- |
| `--open` | 启动后自动打开浏览器 |
| `--port 8890` | 指定起始端口（默认 8890，被占用会自动 +1 顺延，最多试 12 个） |
| `--host 0.0.0.0` | 允许局域网访问（手机等其他设备查看面板） |
| `--check` | 自检：统计数据条数、检查二维码依赖 |

单实例：再次启动会先探测 8890 起 12 个端口，发现是本技能的面板则直接复用并打开浏览器，不会重复起进程。

## 4. 依赖

- Python 3.8+（MarvisAgent 内置 python311 可用）
- `qrcode`、`pillow`（二维码生成）。缺失时面板仍可用，二维码位置会报错，此时执行：
  ```powershell
  pip install qrcode pillow
  ```

## 5. HTTP 接口（面板自用，也可被其他程序调用）

| 接口 | 说明 |
| --- | --- |
| `GET /` | 面板页面 |
| `GET /api/coupons` | 全部优惠数据（JSON，前端自行筛选） |
| `GET /api/reload` | 重新读取 `data/coupons.json`（改完数据不用重启服务） |
| `GET /api/qr?text=<url>` | 实时生成二维码（SVG，前端 CSS 控制大小） |
| `GET /api/health` | 健康检查，返回 `{"ok":true,"owner":"oil-coupon","port":...}` |

## 6. 定时采集（每 15 分钟）

面板已内置 15 分钟自动采集：页面每 900 秒静默重读 `/api/coupons`，比对数据文件 mtime，发现变化即静默刷新表格与筛选器（保留当前筛选态），并在页头提示「检测到数据文件更新，已自动刷新」；切回前台（`visibilitychange`）会立即补刷一次。周期值取 `data/coupons.json` 的 `auto_refresh_sec`（默认 900，可由 `/api/coupons` 下发）。

定时任务（OS 级，与面板解耦，负责真正去网上抓数据写回 json）：

- 任务名：油羊毛优惠数据采集
- 频率：每 15 分钟（cron `*/15 * * * *`）
- 到点由 Marvis 主 Agent 检索各油企/渠道公开优惠信息，与 `data/coupons.json` 增量合并（新增/更新/失效标记），保持字段结构不变，写回后 `/api/reload` 自动生效，面板下一轮轮询即可看到。

## 7. 数据维护（核心）

数据全部在 `data/coupons.json`，改完在面板点「重新读取数据文件」即可生效，无需重启。

### 顶层字段
| 字段 | 说明 |
| --- | --- |
| `updated_at` | 数据整理日期，面板右上角展示 |
| `auto_refresh_sec` | 面板自动采集周期（秒），默认 900（15 分钟） |
| `disclaimer` | 免责声明，面板顶部黄色提示条 |
| `brands` | 品牌字典：`petrochina` 中国石油 / `sinopec` 中国石化 / `both` 两桶油均可 / `channel` 第三方渠道 |
| `regions` | 地区档位：`全国` / `湖北` / `陕西` |
| `grades` | 油号档位：`92` / `95` / `98` |
| `items` | 优惠条目数组 |

### 单条 item 字段
| 字段 | 必填 | 说明 |
| --- | --- | --- |
| `id` | 是 | 唯一标识，建议 `品牌-地区-活动名` |
| `brand` | 是 | `petrochina` / `sinopec` / `both` / `channel` |
| `brand_name` | 是 | 中文品牌名，面板徽标显示 |
| `title` | 是 | 活动名称 |
| `region` | 是 | 必须是 `regions` 里的值 |
| `period` | 是 | 活动时间（含活动周期、每周几、长期/常态化） |
| `status` | 是 | `进行中` / `长期有效` / `待确认` / `部分有效` / `已结束`（面板按此上色，「已结束」整行置灰） |
| `grades` | 是 | 适用油号数组，如 `["92","95","98"]` |
| `benefit` | 是 | 优惠内容（门槛 + 力度，例如「满 220 减 10 元汽油券」） |
| `scope` | 是 | **优惠券支持使用的范围**：站点范围 / 会员等级要求 / 有效期 / 是否叠加 / 是否跨省 |
| `entry` | 是 | 活动入口（APP、公众号、银行 APP 等） |
| `entry_url` | 是 | 活动链接；无公开活动页时用品牌官方入口（中石油 `https://www.cnpc.com.cn`、中石化 `https://www.sinopecsales.com`） |
| `qr_label` | 是 | 二维码下方说明：**扫码后能看到什么 + 真实入口在哪** |
| `source` / `source_url` | 是 | 信息来源与来源链接，保证可追溯 |

### 维护原则（务必遵守）
1. **不编造**：不确定的时间、力度、门槛不要写；宁可标 `待确认`。
2. **过期即标**：活动结束把 `status` 改成 `已结束`，保留作历史参考，不直接删。
3. **写清范围**：`scope` 是用户最关心的字段，必须写清「哪些站能用、什么支付方式、能不能叠加、有效期多久」。
4. **二维码指向可核验的页面**：优先官方活动页；活动页无常驻链接时，指向品牌官方入口并在 `qr_label` 说明真实领取路径。

## 7. 排障

| 现象 | 处理 |
| --- | --- |
| 二维码位置报「二维码生成失败」 | `pip install qrcode pillow`，或忽略（链接可直接点） |
| 端口被占用 | 服务会自动顺延端口；`--check` 前可先用 `--port 8800` 指定区间起点 |
| 浏览器没自动打开 | 手动访问启动日志里打印的 `http://127.0.0.1:<port>/` |
| 面板数据没更新 | 点「重新读取数据文件」，或 `GET /api/reload`；仍不行检查 JSON 语法 |
| 想用手机看 | 以 `--host 0.0.0.0` 启动，手机访问 `http://<电脑局域网 IP>:<port>/` |
| 数据文件损坏 | 服务会返回 `{"ok":false,"error":...}`，面板顶部黄条提示，按提示修 JSON |

## 8. 数据说明与免责

优惠信息来自中国石油 / 中国石化官方渠道、省级销售公司公告、湖北日报、银行官网及主流媒体汇总，整理日期见 `updated_at`。
**本面板非活动主办方**，活动的时间、名额、适用油号与站点范围以官方页面及加油站现场公示为准，加油前请向站点确认。

